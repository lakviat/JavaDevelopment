pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                checkout scm
            }
        }

         stage('Test QA ') {
            steps {
                checkout scm
            }
        }

        stage('Deploy QA ') {
            steps {
                checkout scm
            }
        }
    }
}
